name: Continuous Deployment [DEV]

on:
  push:
    branches:
      - main
    
jobs:
  deploy:
    name: Deploy Dev
    
    uses: beckett-media/auth-lambda-hooks/.github/workflows/cont-deployment.yml@main
    with:
      AWS_REGION: us-west-1
      POST_CONFIRMATION_FUNCTION_NAME: dev-vulcan-vault-congito-post-confirmation-lambda
      PRE_SIGNUP_FUNCTION_NAME: dev-vulcan-vault-congito-pre-signup-lambda
      ENV: dev
    secrets: inherit
